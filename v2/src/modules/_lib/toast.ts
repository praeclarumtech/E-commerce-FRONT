/**
 * Toast notifications styled to match the app's NotificationBar (success/danger/warning/info colors, rounded-lg, border).
 * Uses sonner. Use toast.success(), toast.error(), toast.warning(), toast.info(), or toast(message).
 */
export { toast } from "sonner";
